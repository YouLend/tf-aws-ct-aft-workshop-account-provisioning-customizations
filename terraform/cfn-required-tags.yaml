AWSTemplateFormatVersion: '2010-09-09'
Description: 'Enforce required tags on resources (sample template for workshop)'
Parameters:
  TagMap:
    Type: String
    Description: JSON map of required tag keys and default values
Resources:
  # Placeholder resource – replace with real Config rule in production
  RequiredTagsParameter:
    Type: AWS::SSM::Parameter
    Properties:
      Name: /aft/required-tags
      Type: String
      Value: !Ref TagMap